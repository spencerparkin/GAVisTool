This directory contains sub-directories for all of the platforms that
GAVisTool compiles on.  In each sub-directory you'll find platform-
specific project files for building the project on that platform.