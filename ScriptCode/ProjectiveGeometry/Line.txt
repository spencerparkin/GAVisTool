/*
 * Make a line front two points and another from one point and a vector.
 */
do
(
	p0 = 3*e0 + 3*e1 + 3*e2 + e3,
	p1 = -3*e0 - 3*e1 - 3*e2 + e3,
	lin0 = p0^p1,
	
	/*n = e1,
	lin1 = p0^n,
	
	bind_proj_line( lin0, lin1 ),*/
	
	bind_proj_line( lin0 ),
)